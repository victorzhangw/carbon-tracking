# 問題診斷與快速修復指南

## 📋 問題總結

**日期**：2024-11-23

### 問題 1：重啟後直接進入 Portal

**狀態**：✅ 已找到原因  
**原因**：localStorage 中有舊的登入資料

### 問題 2：AI 廣播劇 UI 未更新

**狀態**：✅ 已找到原因  
**原因**：瀏覽器快取 + 訪問錯誤的 URL

---

## 🔧 問題 1：Portal 登入問題

### 診斷結果

Portal 頁面**已經有**登入檢查代碼：

```javascript
// portal.html 已有此代碼
window.addEventListener("DOMContentLoaded", function () {
  checkLoginStatus(); // 會檢查 localStorage 的 currentUser
});
```

### 問題原因

**localStorage 中有舊的登入資料**：

- 之前登入過，localStorage 儲存了 `currentUser`
- 即使重啟服務，localStorage 不會清除
- Portal 檢查到有 `currentUser` 就認為已登入

### 快速修復方法

#### 方法 1：清除 localStorage（立即生效）

**在瀏覽器 Console 執行**：

```javascript
localStorage.clear();
location.reload();
```

**或手動清除**：

1. 按 `F12` 打開開發者工具
2. 切換到「Application」或「應用程式」標籤
3. 左側選擇「Local Storage」
4. 找到你的網站
5. 刪除 `currentUser` 項目
6. 刷新頁面

#### 方法 2：使用無痕模式測試

```
Ctrl + Shift + N（Chrome/Edge）
Ctrl + Shift + P（Firefox）
```

#### 方法 3：改進登入檢查（建議）

**問題**：目前只檢查 localStorage，不驗證 JWT Token

**改進方案**：

```javascript
// 改進的登入檢查
async function checkLoginStatus() {
  const currentUser = localStorage.getItem("currentUser");
  const accessToken = localStorage.getItem("access_token");

  if (!currentUser || !accessToken) {
    window.location.href =
      "/login?return=" + encodeURIComponent(window.location.pathname);
    return false;
  }

  // 驗證 Token 是否有效
  try {
    const response = await fetch("/api/auth/profile", {
      headers: {
        Authorization: `Bearer ${accessToken}`,
      },
    });

    if (response.status === 401) {
      // Token 過期，清除並跳轉登入
      localStorage.clear();
      window.location.href = "/login";
      return false;
    }

    return true;
  } catch (error) {
    console.error("驗證失敗:", error);
    localStorage.clear();
    window.location.href = "/login";
    return false;
  }
}
```

---

## 🔧 問題 2：AI 廣播劇 UI 未更新

### 診斷結果

AI 廣播劇有**多個版本**的頁面：

| URL                            | 檔案                           | 版本        | 狀態               |
| ------------------------------ | ------------------------------ | ----------- | ------------------ |
| `/api/audiobook/`              | `audiobook_player.html`        | 舊版        | ❌ 無 Phase 1 功能 |
| `/api/audiobook/test`          | `audiobook_test.html`          | 測試版      | ❌ 無 Phase 1 功能 |
| `/api/audiobook/test-enhanced` | `audiobook_test_enhanced.html` | **Phase 1** | ✅ 有新功能        |

### 問題原因

1. **訪問了錯誤的 URL**

   - 如果訪問 `/api/audiobook/` 會看到舊版
   - 應該訪問 `/api/audiobook/test-enhanced`

2. **瀏覽器快取**
   - CSS/JS 檔案被快取
   - 即使更新了檔案，瀏覽器仍使用舊版

### 快速修復方法

#### 步驟 1：訪問正確的 URL

```
✅ 正確：http://localhost:5000/api/audiobook/test-enhanced
❌ 錯誤：http://localhost:5000/api/audiobook/
```

#### 步驟 2：清除瀏覽器快取

**方法 A：強制重新載入**

```
Ctrl + F5（Windows）
Cmd + Shift + R（Mac）
```

**方法 B：清除快取**

```
1. Ctrl + Shift + Delete
2. 選擇「快取的圖片和檔案」
3. 點擊「清除資料」
```

**方法 C：使用無痕模式**

```
Ctrl + Shift + N
```

#### 步驟 3：驗證檔案存在

```bash
# 檢查檔案是否存在
dir static\css\audiobook_enhanced.css
dir static\js\audiobook_player_enhanced.js
dir templates\audiobook_test_enhanced.html
```

#### 步驟 4：檢查檔案內容

確認 `audiobook_test_enhanced.html` 有引入新的 CSS/JS：

```html
<!-- 應該有這些引入 -->
<link rel="stylesheet" href="/static/css/audiobook_enhanced.css" />
<script src="/static/js/audiobook_player_enhanced.js"></script>
```

---

## 📊 Phase 1 功能對照表

### AI 廣播劇 Phase 1 新功能

| 功能         | 舊版 | Phase 1 |
| ------------ | ---- | ------- |
| 播放進度追蹤 | ❌   | ✅      |
| 書籤功能     | ❌   | ✅      |
| 播放速度調整 | ❌   | ✅ 0.8x |
| 用戶偏好設定 | ❌   | ✅      |
| 資料庫儲存   | ❌   | ✅      |
| 增強版播放器 | ❌   | ✅      |

### 如何確認是 Phase 1 版本

**檢查點**：

1. 頁面標題應該有「Phase 1」或「增強版」
2. 有播放進度條
3. 有書籤按鈕
4. 有速度調整選項
5. 播放速度預設為 0.8x

---

## 🎯 完整測試流程

### 測試 Portal 登入

```
1. 清除 localStorage
   localStorage.clear();

2. 訪問 Portal
   http://localhost:5000/portal

3. 應該跳轉到登入頁面
   http://localhost:5000/login?return=/portal

4. 登入後應該返回 Portal
```

### 測試 AI 廣播劇

```
1. 清除瀏覽器快取
   Ctrl + Shift + Delete

2. 訪問 Phase 1 版本
   http://localhost:5000/api/audiobook/test-enhanced

3. 檢查新功能
   - 播放進度追蹤
   - 書籤功能
   - 速度調整

4. 檢查播放速度
   - 應該預設為 0.8x
```

---

## 🔍 進階診斷

### 檢查 localStorage

```javascript
// 在瀏覽器 Console 執行
console.log("currentUser:", localStorage.getItem("currentUser"));
console.log("access_token:", localStorage.getItem("access_token"));
console.log("refresh_token:", localStorage.getItem("refresh_token"));
```

### 檢查 JWT Token

```javascript
// 解碼 JWT Token（需要 jwt-decode 庫或線上工具）
const token = localStorage.getItem("access_token");
console.log("Token:", token);

// 檢查 Token 是否過期
const payload = JSON.parse(atob(token.split(".")[1]));
const exp = new Date(payload.exp * 1000);
console.log("Token 過期時間:", exp);
console.log("是否已過期:", exp < new Date());
```

### 檢查靜態檔案

```bash
# 檢查檔案修改時間
dir /T:W static\css\audiobook_enhanced.css
dir /T:W static\js\audiobook_player_enhanced.js

# 檢查檔案大小
dir static\css\audiobook_enhanced.css
dir static\js\audiobook_player_enhanced.js
```

---

## 📝 快速修復清單

### Portal 登入問題

- [ ] 清除 localStorage
- [ ] 刷新頁面
- [ ] 確認跳轉到登入頁面
- [ ] 登入後確認返回 Portal

### AI 廣播劇 UI 問題

- [ ] 訪問正確的 URL (`/api/audiobook/test-enhanced`)
- [ ] 清除瀏覽器快取
- [ ] 強制重新載入（Ctrl + F5）
- [ ] 確認看到 Phase 1 功能
- [ ] 確認播放速度為 0.8x

---

## 🎉 預期結果

### Portal 登入

```
未登入狀態：
http://localhost:5000/portal
  ↓
自動跳轉
  ↓
http://localhost:5000/login?return=/portal
```

### AI 廣播劇

```
訪問 Phase 1 版本：
http://localhost:5000/api/audiobook/test-enhanced
  ↓
看到新功能：
- ✅ 播放進度追蹤
- ✅ 書籤功能
- ✅ 速度調整（0.8x）
- ✅ 增強版 UI
```

---

## 💡 預防措施

### 1. 改進登入檢查

在 `portal.html` 添加 Token 驗證：

```javascript
// 不只檢查 localStorage，還要驗證 Token
async function checkLoginStatus() {
  // 檢查 Token 是否有效
  const response = await fetch("/api/auth/profile", {
    headers: { Authorization: `Bearer ${token}` },
  });

  if (response.status === 401) {
    localStorage.clear();
    window.location.href = "/login";
  }
}
```

### 2. 添加版本號到靜態檔案

```html
<!-- 添加版本號防止快取 -->
<link rel="stylesheet" href="/static/css/audiobook_enhanced.css?v=1.0" />
<script src="/static/js/audiobook_player_enhanced.js?v=1.0"></script>
```

### 3. 統一 AI 廣播劇入口

建議將 Phase 1 版本設為預設：

```python
@audiobook_bp.route('/')
def audiobook_home():
    # 重定向到 Phase 1 版本
    return redirect('/api/audiobook/test-enhanced')
```

---

**診斷人員**：AI 語音互動平台開發團隊  
**診斷日期**：2024-11-23  
**狀態**：✅ 問題已診斷，修復方案已提供
