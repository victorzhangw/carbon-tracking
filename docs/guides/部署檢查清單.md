# ✅ 部署前檢查清單

## 📦 檔案準備（已完成）

- [x] `config/requirements/base.txt` - Python 套件清單
- [x] `config/deployment/render.yaml` - Render 部署設定
- [x] `app.py` - 支援環境變數的啟動檔
- [x] `android_app/app/src/main/java/com/carbontracking/app/MainActivity.kt` - 已更新雲端網址

## 🚀 立即行動（今天要做）

### 1️⃣ 部署後端到 Render（30 分鐘）

```bash
# Step 1: 初始化 Git（如果還沒有）
git init
git add .
git commit -m "準備部署碳排放追蹤系統"

# Step 2: 推送到 GitHub
# 先到 GitHub 建立 repository: carbon-tracking
git remote add origin https://github.com/你的用戶名/carbon-tracking.git
git branch -M main
git push -u origin main

# Step 3: 到 Render 部署
# 1. 訪問 https://render.com
# 2. 用 GitHub 帳號註冊/登入
# 3. New + > Web Service
# 4. 連接你的 repository
# 5. 設定：
#    - Name: carbon-tracking
#    - Environment: Python 3
#    - Build: pip install -r config/requirements/base.txt
#    - Start: python app.py
# 6. Create Web Service
# 7. 等待 5-10 分鐘部署完成
```

**部署完成後你會獲得網址：**

```
https://carbon-tracking.onrender.com
```

### 2️⃣ 測試部署（5 分鐘）

```bash
# 在瀏覽器訪問
https://carbon-tracking.onrender.com/carbon/

# 測試功能：
# ✅ 首頁載入
# ✅ 新增記錄
# ✅ 查看統計
# ✅ 匯出 Excel
```

### 3️⃣ 建置 Android APK（20 分鐘）

```bash
cd android_app

# 建立簽署金鑰（只需一次）
keytool -genkey -v -keystore carbon-tracking.keystore -alias carbon -keyalg RSA -keysize 2048 -validity 10000

# 輸入資訊：
# - 密碼：設定一個安全的密碼（記住它！）
# - 姓名：你的名字
# - 組織：你的組織名稱
# - 城市：台灣
# - 省份：Taiwan
# - 國家代碼：TW

# 建置 Release APK
gradlew.bat assembleRelease

# APK 位置：
# app/build/outputs/apk/release/app-release.apk
```

### 4️⃣ 測試 APK（10 分鐘）

```bash
# 連接手機，啟用 USB 偵錯
adb devices

# 安裝 APK
adb install app/build/outputs/apk/release/app-release.apk

# 在手機上測試所有功能
```

## 📱 明天要做（準備上架素材）

### 1. 製作 Feature Graphic

- 尺寸：1024 x 500 px
- 格式：PNG 或 JPG
- 內容：App 名稱 + 主要功能

### 2. 截取 Screenshots

- 至少 2 張，最多 8 張
- 尺寸：手機實際解析度
- 內容：主要功能畫面

### 3. 準備文案

- App 簡短描述（80 字內）
- App 完整描述（4000 字內）
- 隱私權政策網址

### 4. 註冊 Google Play 開發者

- 費用：$25 美金（一次性）
- 網址：https://play.google.com/console

## 🚀 後天要做（上架）

### 1. 建立應用程式

- 登入 Google Play Console
- 建立新應用程式
- 填寫基本資訊

### 2. 上傳 AAB

```bash
cd android_app
gradlew.bat bundleRelease
# 上傳：app/build/outputs/bundle/release/app-release.aab
```

### 3. 提交審核

- 填寫內容分級問卷
- 設定定價與發行國家
- 提交審核（通常 1-3 天）

## 🎯 目前進度

- [x] 後端代碼完成
- [x] Android App 完成
- [x] 部署檔案準備完成
- [ ] 部署到 Render
- [ ] 建置 APK
- [ ] 測試 APK
- [ ] 準備上架素材
- [ ] 上架 Google Play

## 💡 小提示

1. **Render Free 方案**：15 分鐘無活動會休眠，首次喚醒需 30-60 秒
2. **簽署金鑰**：一定要備份！遺失後無法更新 App
3. **測試**：在實體手機上測試所有功能
4. **隱私權政策**：已包含在 `templates/privacy_policy.html`

## 🆘 需要協助？

遇到問題隨時問我：

- 部署失敗
- 建置錯誤
- 上架問題
- 任何技術問題

**準備好開始了嗎？從第一步開始吧！** 🚀
