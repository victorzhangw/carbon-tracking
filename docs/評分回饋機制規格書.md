# 📊 評分回饋機制規格書

## 📋 功能概述

在情緒識別系統中添加對話評分功能，讓用戶可以結束對話並獲得綜合評分報告，包含情緒分析、語音表現、文字內容等多維度評估。

---

## 🎯 功能目標

### 主要目標

1. **對話管理** - 提供停止對話按鈕
2. **多維度評分** - 情緒、語音、文字三個維度
3. **視覺化呈現** - 雷達圖、進度條、評分卡片
4. **建議反饋** - 根據評分提供改進建議
5. **歷史記錄** - 保存評分歷史供查看

### 次要目標

- 匯出評分報告（PDF/圖片）
- 評分趨勢分析
- 與其他用戶比較

---

## 🎨 UI/UX 設計

### 1. 停止對話按鈕

#### 位置

- 在錄音控制區域旁邊
- 顯眼但不干擾主要操作

#### 樣式

```css
.end-session-btn {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  padding: 12px 24px;
  border-radius: 8px;
  font-size: 16px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
}

.end-session-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 16px rgba(102, 126, 234, 0.3);
}
```

#### 狀態

- **初始**: 灰色禁用（至少需要 1 次對話）
- **可用**: 漸變紫色
- **點擊**: 顯示確認對話框

### 2. 評分報告彈窗

#### 結構

```
┌─────────────────────────────────────┐
│  🎉 對話評分報告                     │
├─────────────────────────────────────┤
│                                     │
│  [雷達圖]                           │
│                                     │
├─────────────────────────────────────┤
│  📊 詳細評分                         │
│                                     │
│  情緒表達  ████████░░ 80/100       │
│  語音品質  ██████████ 95/100       │
│  文字內容  ███████░░░ 75/100       │
│                                     │
│  綜合評分: 83/100 ⭐⭐⭐⭐          │
├─────────────────────────────────────┤
│  💡 改進建議                         │
│  • 建議1...                         │
│  • 建議2...                         │
├─────────────────────────────────────┤
│  [關閉] [查看詳情] [匯出報告]       │
└─────────────────────────────────────┘
```

---

## 📊 評分維度

### 1. 情緒表達評分 (0-100)

#### 評分標準

| 分數範圍 | 等級   | 說明                     |
| -------- | ------ | ------------------------ |
| 90-100   | 優秀   | 情緒表達豐富、自然、恰當 |
| 75-89    | 良好   | 情緒表達清晰，偶有不自然 |
| 60-74    | 及格   | 情緒表達基本清晰         |
| 0-59     | 待改進 | 情緒表達單一或不清晰     |

#### 評分因子

```javascript
emotionScore = {
  diversity: 30, // 情緒多樣性 (30%)
  appropriateness: 40, // 情緒恰當性 (40%)
  intensity: 20, // 情緒強度 (20%)
  consistency: 10, // 情緒一致性 (10%)
};
```

#### 計算邏輯

```python
def calculate_emotion_score(conversation_history):
    # 情緒多樣性：出現的不同情緒種類
    unique_emotions = len(set([msg['sentiment'] for msg in conversation_history]))
    diversity_score = min(unique_emotions * 10, 30)

    # 情緒恰當性：情緒與內容的匹配度
    appropriateness_score = analyze_emotion_appropriateness(conversation_history)

    # 情緒強度：情緒的明確程度
    intensity_score = calculate_emotion_intensity(conversation_history)

    # 情緒一致性：情緒轉換的合理性
    consistency_score = calculate_emotion_consistency(conversation_history)

    total_score = (
        diversity_score +
        appropriateness_score +
        intensity_score +
        consistency_score
    )

    return min(total_score, 100)
```

### 2. 語音品質評分 (0-100)

#### 評分標準

| 分數範圍 | 等級   | 說明                         |
| -------- | ------ | ---------------------------- |
| 90-100   | 優秀   | 發音清晰、音量適中、語速適當 |
| 75-89    | 良好   | 發音清晰，偶有不清楚         |
| 60-74    | 及格   | 基本可理解                   |
| 0-59     | 待改進 | 發音不清或音量問題           |

#### 評分因子

```javascript
voiceScore = {
  clarity: 40, // 清晰度 (40%)
  volume: 20, // 音量適中度 (20%)
  speed: 20, // 語速適當性 (20%)
  fluency: 20, // 流暢度 (20%)
};
```

#### 計算邏輯

```python
def calculate_voice_score(audio_features):
    # 清晰度：基於 ASR 信心分數
    clarity_score = audio_features['confidence'] * 40

    # 音量：檢查音量是否在合理範圍
    volume_score = calculate_volume_score(audio_features['volume'])

    # 語速：檢查語速是否適中
    speed_score = calculate_speed_score(audio_features['words_per_minute'])

    # 流暢度：檢查停頓和重複
    fluency_score = calculate_fluency_score(audio_features['pauses'])

    total_score = (
        clarity_score +
        volume_score +
        speed_score +
        fluency_score
    )

    return min(total_score, 100)
```

### 3. 文字內容評分 (0-100)

#### 評分標準

| 分數範圍 | 等級   | 說明                         |
| -------- | ------ | ---------------------------- |
| 90-100   | 優秀   | 內容豐富、表達清晰、邏輯連貫 |
| 75-89    | 良好   | 內容清晰，表達流暢           |
| 60-74    | 及格   | 基本表達清楚                 |
| 0-59     | 待改進 | 內容簡單或表達不清           |

#### 評分因子

```javascript
contentScore = {
  length: 20, // 內容長度 (20%)
  richness: 30, // 內容豐富度 (30%)
  coherence: 30, // 邏輯連貫性 (30%)
  engagement: 20, // 互動參與度 (20%)
};
```

#### 計算邏輯

```python
def calculate_content_score(conversation_history):
    # 內容長度：平均每次對話的字數
    avg_length = calculate_average_length(conversation_history)
    length_score = min(avg_length / 2, 20)  # 假設理想長度為40字

    # 內容豐富度：詞彙多樣性
    richness_score = calculate_vocabulary_diversity(conversation_history)

    # 邏輯連貫性：對話的連貫性
    coherence_score = calculate_coherence(conversation_history)

    # 互動參與度：對話輪次和回應質量
    engagement_score = calculate_engagement(conversation_history)

    total_score = (
        length_score +
        richness_score +
        coherence_score +
        engagement_score
    )

    return min(total_score, 100)
```

### 4. 綜合評分

```python
def calculate_overall_score(emotion_score, voice_score, content_score):
    # 加權平均
    weights = {
        'emotion': 0.35,   # 情緒 35%
        'voice': 0.35,     # 語音 35%
        'content': 0.30    # 內容 30%
    }

    overall = (
        emotion_score * weights['emotion'] +
        voice_score * weights['voice'] +
        content_score * weights['content']
    )

    return round(overall, 1)
```

---

## 💡 改進建議系統

### 建議生成邏輯

```python
def generate_suggestions(scores):
    suggestions = []

    # 情緒表達建議
    if scores['emotion'] < 75:
        if scores['emotion_diversity'] < 20:
            suggestions.append({
                'category': '情緒表達',
                'icon': '😊',
                'text': '嘗試表達更多樣的情緒，讓對話更生動',
                'priority': 'high'
            })
        if scores['emotion_intensity'] < 15:
            suggestions.append({
                'category': '情緒表達',
                'icon': '💪',
                'text': '可以更明確地表達你的感受和情緒',
                'priority': 'medium'
            })

    # 語音品質建議
    if scores['voice'] < 75:
        if scores['clarity'] < 30:
            suggestions.append({
                'category': '語音品質',
                'icon': '🎤',
                'text': '說話時可以更清晰一些，注意發音',
                'priority': 'high'
            })
        if scores['volume'] < 15:
            suggestions.append({
                'category': '語音品質',
                'icon': '🔊',
                'text': '調整音量，保持在適中範圍',
                'priority': 'medium'
            })
        if scores['speed'] < 15:
            suggestions.append({
                'category': '語音品質',
                'icon': '⏱️',
                'text': '調整語速，不要太快或太慢',
                'priority': 'medium'
            })

    # 文字內容建議
    if scores['content'] < 75:
        if scores['length'] < 15:
            suggestions.append({
                'category': '文字內容',
                'icon': '📝',
                'text': '可以說得更詳細一些，豐富對話內容',
                'priority': 'medium'
            })
        if scores['richness'] < 20:
            suggestions.append({
                'category': '文字內容',
                'icon': '📚',
                'text': '嘗試使用更多樣的詞彙和表達方式',
                'priority': 'low'
            })
        if scores['coherence'] < 20:
            suggestions.append({
                'category': '文字內容',
                'icon': '🔗',
                'text': '注意對話的邏輯連貫性',
                'priority': 'high'
            })

    # 按優先級排序
    priority_order = {'high': 0, 'medium': 1, 'low': 2}
    suggestions.sort(key=lambda x: priority_order[x['priority']])

    return suggestions[:5]  # 最多返回5條建議
```

---

## 🎨 視覺化設計

### 1. 雷達圖

```javascript
// 使用 Chart.js
const radarData = {
  labels: ["情緒表達", "語音品質", "文字內容", "互動參與", "整體表現"],
  datasets: [
    {
      label: "本次評分",
      data: [80, 95, 75, 85, 83],
      backgroundColor: "rgba(102, 126, 234, 0.2)",
      borderColor: "rgba(102, 126, 234, 1)",
      borderWidth: 2,
    },
  ],
};
```

### 2. 進度條

```html
<div class="score-bar">
  <div class="score-label">情緒表達</div>
  <div class="score-progress">
    <div class="score-fill" style="width: 80%"></div>
  </div>
  <div class="score-value">80/100</div>
</div>
```

### 3. 評分卡片

```html
<div class="score-card">
  <div class="score-icon">😊</div>
  <div class="score-title">情緒表達</div>
  <div class="score-number">80</div>
  <div class="score-grade">良好</div>
</div>
```

---

## 🔧 技術實現

### 後端 API

#### 1. 結束對話並評分

```python
@main.route('/api/end-session', methods=['POST'])
def end_session():
    """結束對話並生成評分報告"""
    try:
        data = request.get_json()
        session_id = data.get('session_id')
        conversation_history = data.get('conversation_history', [])

        # 計算各維度評分
        emotion_score = calculate_emotion_score(conversation_history)
        voice_score = calculate_voice_score(conversation_history)
        content_score = calculate_content_score(conversation_history)
        overall_score = calculate_overall_score(
            emotion_score, voice_score, content_score
        )

        # 生成改進建議
        suggestions = generate_suggestions({
            'emotion': emotion_score,
            'voice': voice_score,
            'content': content_score
        })

        # 保存評分記錄
        save_score_record(session_id, {
            'emotion_score': emotion_score,
            'voice_score': voice_score,
            'content_score': content_score,
            'overall_score': overall_score,
            'conversation_count': len(conversation_history),
            'timestamp': datetime.now()
        })

        return jsonify({
            'status': 'success',
            'scores': {
                'emotion': emotion_score,
                'voice': voice_score,
                'content': content_score,
                'overall': overall_score
            },
            'grade': get_grade(overall_score),
            'suggestions': suggestions,
            'statistics': {
                'conversation_count': len(conversation_history),
                'total_words': sum([len(msg['text']) for msg in conversation_history]),
                'duration': calculate_duration(conversation_history)
            }
        })

    except Exception as e:
        return jsonify({'status': 'error', 'message': str(e)}), 500
```

#### 2. 獲取歷史評分

```python
@main.route('/api/score-history', methods=['GET'])
def get_score_history():
    """獲取歷史評分記錄"""
    try:
        user_id = request.args.get('user_id', 'default')
        limit = int(request.args.get('limit', 10))

        records = get_user_score_records(user_id, limit)

        return jsonify({
            'status': 'success',
            'records': records,
            'statistics': {
                'average_score': calculate_average(records),
                'best_score': max([r['overall_score'] for r in records]),
                'improvement': calculate_improvement(records)
            }
        })

    except Exception as e:
        return jsonify({'status': 'error', 'message': str(e)}), 500
```

### 前端實現

#### 1. 對話歷史追蹤

```javascript
// 全局變量
let conversationHistory = [];
let sessionStartTime = null;

// 記錄對話
function recordConversation(type, text, sentiment, audioFeatures) {
  conversationHistory.push({
    type: type, // 'user' or 'ai'
    text: text,
    sentiment: sentiment,
    audioFeatures: audioFeatures,
    timestamp: new Date(),
  });
}
```

#### 2. 結束對話

```javascript
async function endSession() {
  // 確認對話框
  const confirmed = await showConfirmDialog(
    "確定要結束對話嗎？",
    "系統將為您生成評分報告"
  );

  if (!confirmed) return;

  // 顯示載入動畫
  showLoading("正在生成評分報告...");

  try {
    const response = await fetch("/api/end-session", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        session_id: sessionId,
        conversation_history: conversationHistory,
      }),
    });

    const result = await response.json();

    if (result.status === "success") {
      // 顯示評分報告
      showScoreReport(result);
    } else {
      showError("生成評分報告失敗");
    }
  } catch (error) {
    console.error("結束對話失敗:", error);
    showError("系統錯誤，請稍後再試");
  } finally {
    hideLoading();
  }
}
```

#### 3. 顯示評分報告

```javascript
function showScoreReport(data) {
  const modal = createScoreModal();

  // 渲染雷達圖
  renderRadarChart(modal, data.scores);

  // 渲染評分卡片
  renderScoreCards(modal, data.scores);

  // 渲染改進建議
  renderSuggestions(modal, data.suggestions);

  // 渲染統計資訊
  renderStatistics(modal, data.statistics);

  // 顯示彈窗
  modal.show();
}
```

---

## 📱 響應式設計

### 桌面版 (>768px)

- 雷達圖 + 評分卡片並排
- 建議列表 3 列顯示

### 平板版 (768px-1024px)

- 雷達圖上方，評分卡片下方
- 建議列表 2 列顯示

### 手機版 (<768px)

- 垂直排列所有元素
- 建議列表 1 列顯示
- 簡化雷達圖

---

## 🎯 評分等級

| 分數範圍 | 等級 | 星級       | 顏色 | 稱號   |
| -------- | ---- | ---------- | ---- | ------ |
| 95-100   | S    | ⭐⭐⭐⭐⭐ | 金色 | 大師級 |
| 85-94    | A    | ⭐⭐⭐⭐   | 紫色 | 優秀   |
| 75-84    | B    | ⭐⭐⭐     | 藍色 | 良好   |
| 60-74    | C    | ⭐⭐       | 綠色 | 及格   |
| 0-59     | D    | ⭐         | 灰色 | 待改進 |

---

## 📊 數據存儲

### 資料庫結構

```sql
CREATE TABLE score_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id TEXT NOT NULL,
    user_id TEXT DEFAULT 'default',
    emotion_score REAL NOT NULL,
    voice_score REAL NOT NULL,
    content_score REAL NOT NULL,
    overall_score REAL NOT NULL,
    grade TEXT NOT NULL,
    conversation_count INTEGER NOT NULL,
    total_words INTEGER NOT NULL,
    duration INTEGER NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE score_details (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    record_id INTEGER NOT NULL,
    category TEXT NOT NULL,
    sub_score REAL NOT NULL,
    FOREIGN KEY (record_id) REFERENCES score_records(id)
);
```

---

## 🚀 實施計劃

### Phase 1: 基礎功能 (1-2 天)

- [ ] 添加停止對話按鈕
- [ ] 實現對話歷史追蹤
- [ ] 基礎評分計算邏輯
- [ ] 簡單評分報告彈窗

### Phase 2: 評分優化 (2-3 天)

- [ ] 完善評分算法
- [ ] 添加雷達圖視覺化
- [ ] 實現改進建議系統
- [ ] 評分等級和稱號

### Phase 3: 進階功能 (2-3 天)

- [ ] 歷史記錄查看
- [ ] 評分趨勢分析
- [ ] 匯出報告功能
- [ ] 數據統計儀表板

### Phase 4: 優化完善 (1-2 天)

- [ ] 響應式設計優化
- [ ] 動畫效果添加
- [ ] 性能優化
- [ ] 測試和修復

---

## ✅ 驗收標準

### 功能性

- [ ] 停止對話按鈕正常工作
- [ ] 評分計算準確
- [ ] 報告顯示完整
- [ ] 建議生成合理

### 性能

- [ ] 評分計算 < 2 秒
- [ ] 報告渲染 < 1 秒
- [ ] 無明顯卡頓

### 用戶體驗

- [ ] 操作流暢直觀
- [ ] 視覺效果美觀
- [ ] 建議實用有幫助
- [ ] 響應式設計良好

---

**文檔版本**: 1.0  
**創建日期**: 2025-11-21  
**狀態**: 待實施
